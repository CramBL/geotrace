# These directories contain byte-exact test data, the captures and fixtures
# defined in CODE_STYLE.md's vocabulary table. Checkout must never rewrite
# their line endings: the tests read them byte for byte, and git's text
# heuristic CRLF-converts the NUL-free ones on Windows.
**/tests/captures/** -text
**/tests/fixtures/** -text
