[package]
name = "gt-history"
description.workspace = true
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[lib]
doctest = false
test = false

[features]
# The system-HDF5 backend is the default: it edits the history file in place
# (insert/delete without rewriting the whole file) and tracks free space. The
# pure-Rust backend stays available as an opt-in, zero-C-dependency fallback.
# It is not pulled into the default build.
default = ["backend-sys"]
backend-pure = ["dep:gt-history-backend-pure"]
backend-sys = ["dep:gt-history-backend-sys"]

[dependencies]
dirs.workspace = true
gt-history-backend-pure = { path = "../gt-history-backend-pure", optional = true }
gt-history-backend-sys = { path = "../gt-history-backend-sys", optional = true }
gt-history-types = { path = "../gt-history-types" }

[dev-dependencies]
geotrace-sdk = { path = "../../sdk/rust/geotrace-sdk" }
hdf5 = { package = "hdf5-metno", version = "0.13" }
hdf5-pure.workspace = true
insta.workspace = true
log.workspace = true
tempfile.workspace = true
test-log.workspace = true

[lints]
workspace = true
